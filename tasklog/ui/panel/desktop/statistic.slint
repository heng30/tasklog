import { Theme, Icons, Store, Util, Logic } from "../def.slint";
import { FlickableWithScrollBar, DatePicker, DatePickerPopup, Label, HorizontalLedNumber, VerticalLedNumber } from "../../base/widgets.slint";

export component Statistic inherits VerticalLayout {
    spacing: Theme.spacing * 8;

    FlickableWithScrollBar {
        VerticalLayout {
            spacing: Theme.spacing * 2;
            for index in 10: Rectangle {
                background: green;
                height: 100px;

                Label {
                    text: index;
                    font-size: Theme.title1-font-size;
                }
            }
        }
    }
}
